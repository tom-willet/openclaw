# Polymarket Configuration

# Dry run mode (set to false for live trading)
POLYMARKET_DRY_RUN=true

# API endpoints
POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws/market

# Risk limits (dry run mode)
POLYMARKET_MAX_POSITION_SIZE=100
POLYMARKET_CHECK_INTERVAL=60

# Logging
POLYMARKET_LOG_LEVEL=info

# === CHAINLINK DATA STREAMS (OPTIONAL) ===
# Official Polymarket settlement source - eliminates basis risk
# ✨ Agent works great with Binance alone! (+$43, 100% win first run)
# 🎯 Chainlink just adds precision by matching settlement source
# Sign up at: https://data.chain.link (free testnet available)
#
# CHAINLINK_API_KEY=your_api_key_here
# CHAINLINK_API_SECRET=your_api_secret_here

# Optional: Override default endpoints (testnet by default)
# Testnet (default):
# CHAINLINK_ENDPOINT=https://api.testnet-dataengine.chain.link
# CHAINLINK_WS_ENDPOINT=wss://ws.testnet-dataengine.chain.link
#
# Mainnet (production):
# CHAINLINK_ENDPOINT=https://api.dataengine.chain.link
# CHAINLINK_WS_ENDPOINT=wss://ws.dataengine.chain.link
